---
# Ansible Collections Requirements
# Installieren mit: ansible-galaxy collection install -r requirements.yml

collections:
  - name: ansible.posix
    version: ">=1.5.0"

  - name: community.postgresql
    version: ">=3.0.0"

  - name: community.general
    version: ">=7.0.0"
